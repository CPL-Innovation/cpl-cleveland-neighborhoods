import { StaffApp } from "@/components/staff/app";

// The staff enrichment + scan surface. StaffApp is a client SPA (NavContext router).
export default function StaffPage() {
  return <StaffApp />;
}
